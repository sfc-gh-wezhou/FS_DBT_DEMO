version: 2

models:
  - name: transactions
    description: My transformation written in Python
    config:
      database: SNOWML_FEATURE_STORE_DBT_DEMO
      schema: FS_DBT_DEMO
      materialized: table
      tags: ['python']

  - name: customers
    description: My transformation written in Python
    config:
      database: SNOWML_FEATURE_STORE_DBT_DEMO
      schema: FS_DBT_DEMO
      materialized: table
      tags: ['python']

  - name: terminals
    description: My transformation written in Python
    config:
      database: SNOWML_FEATURE_STORE_DBT_DEMO
      schema: FS_DBT_DEMO
      materialized: table
      tags: ['python']