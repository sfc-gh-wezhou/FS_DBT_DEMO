version: 2

models:
  - name: transactions
    description: My transformation written in Python
    config:
      database: DBT_COALESCE
      schema: _FEATURES
      materialized: table
      tags: ['python']

  - name: customers
    description: My transformation written in Python
    config:
      database: DBT_COALESCE
      schema: _FEATURES
      materialized: table
      tags: ['python']

  - name: terminals
    description: My transformation written in Python
    config:
      database: DBT_COALESCE
      schema: _FEATURES
      materialized: table
      tags: ['python']